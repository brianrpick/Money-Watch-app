$('#calendar-one').fullCalendar('removeEvents', [<%= @expense.id %>]);
$('#calendar-one').fullCalendar(
  'renderEvent', 
  $.parseJSON("<%=j render(@expense, format: :json).html_safe %>"), 
  true
);
$('.modal').modal('hide');